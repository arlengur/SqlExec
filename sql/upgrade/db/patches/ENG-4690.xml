<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="ENG-4690-01" author="nfleming">
        <comment>
            Create join table to associate organizations with categories
        </comment>

        <sql>
            CREATE TABLE `prodlims`.`organization_category` (
                organization_id BIGINT(20) NOT NULL,
                categories_id BIGINT(20) NOT NULL,

                PRIMARY KEY (organization_id, categories_id)
            );
        </sql>

        <rollback>
            DROP TABLE IF EXISTS `prodlims`.`organization_category`;
        </rollback>
    </changeSet>

</databaseChangeLog>