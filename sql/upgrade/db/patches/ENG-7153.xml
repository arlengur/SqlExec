<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="ENG-7153-01" author="mdaly">
        <comment>Migrate existing results to new values; primarily to help SQA</comment>
        <sql>
            SET SQL_SAFE_UPDATES = 0;
            UPDATE `prodlims`.`horizoncloudresult` SET `zygosity`='HET' WHERE `zygosity`='HETEROZYGOSITY';
            UPDATE `prodlims`.`horizoncloudresult` SET `zygosity`='HOM' WHERE `zygosity`='HOMOZYGOSITY';
            UPDATE `prodlims`.`horizoncloudresult` SET `zygosity`='UNDETERMINED' WHERE `zygosity`='OTHER';
            SET SQL_SAFE_UPDATES = 1;
        </sql>

        <rollback>
            SET SQL_SAFE_UPDATES = 0;
            UPDATE `prodlims`.`horizoncloudresult` SET `zygosity`='HETEROZYGOSITY' WHERE `zygosity`='HET';
            UPDATE `prodlims`.`horizoncloudresult` SET `zygosity`='HOMOZYGOSITY' WHERE `zygosity`='HOM';
            UPDATE `prodlims`.`horizoncloudresult` SET `zygosity`='OTHER' WHERE `zygosity`='UNDETERMINED';
            SET SQL_SAFE_UPDATES = 1;
        </rollback>
    </changeSet>
</databaseChangeLog>