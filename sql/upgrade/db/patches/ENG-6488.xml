<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="ENG-6488-01" author="jdavid">
        <comment>Create sample identity table</comment>
        <sql>

            CREATE TABLE `prodlims`.`sampleidentity` (
            `id` bigint(20) NOT NULL AUTO_INCREMENT,
            `sample_id` bigint(20) NOT NULL,
            `value` varchar(255) NOT NULL,
            PRIMARY KEY (`id`),
            KEY `FK_SAMPLEIDENTITY_SAMPLE_ID` (`sample_id`),
            CONSTRAINT `FK_SAMPLEIDENTITY_SAMPLE_ID` FOREIGN KEY (`sample_id`) REFERENCES `sample` (`id`)
            ) ENGINE=InnoDB DEFAULT CHARSET=latin1;

        </sql>
        <rollback>
            drop table if exists `prodlims`.`sampleidentity`;
        </rollback>
    </changeSet>


</databaseChangeLog>