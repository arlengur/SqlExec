<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="ENG-7960" author="mdubrovin">
        <comment>Create privacystandard table</comment>
        <sql>

            CREATE TABLE `privacystandard` (
            `id` bigint(20) NOT NULL AUTO_INCREMENT,
            `country_code` varchar(2) NOT NULL,
            `standard` varchar(30) NOT NULL,
            PRIMARY KEY (`id`),
            UNIQUE KEY `UK_privacy_standard_standard_and_code` (`country_code`, `standard`)
            );

        </sql>
        <rollback>
            DROP TABLE IF EXISTS `privacystandard`;
        </rollback>
    </changeSet>

</databaseChangeLog>
