<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="ENG-5987-01" author="hharidas">
        <comment>Ability to Mark report as updated and add on reports.</comment>
        <sql>
            ALTER TABLE `prodlims`.`horizon_report` ADD COLUMN `updatedReport` BIT(1) NOT NULL DEFAULT b'0' AFTER `revisedReport`;
        </sql>
        <rollback>
            ALTER TABLE `prodlims`.`horizon_report` DROP COLUMN `updatedReport`;
        </rollback>
    </changeSet>

</databaseChangeLog>